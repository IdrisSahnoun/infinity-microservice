export interface Mic {
    id: number;
    name: string;
    description: string;
    startDate: string; // ISO string format (e.g., "2025-04-14T10:00:00")
    endDate: string;
    location: string;
  }